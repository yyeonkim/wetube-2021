extends ../base
include ../mixins/video

block content
    div.profile__data
        a(href="/users/edit")
            if (isHeroku)
                img(src=user.avatarUrl).avatar 
            else 
                img(src="/" + user.avatarUrl).avatar
            h3=user.username
    div.video-grid
        each video in user.videos 
            +video(video)
        else 
            span.empty__message 아직 동영상이 없습니다.