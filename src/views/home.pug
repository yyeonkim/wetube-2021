extends base
include mixins/video

block content
    div.video-grid
        each video in videos
            +video(video)
            
        else        
            span.empty__message 아직 업로드된 동영상이 없습니다.
